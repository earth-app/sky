# maestro requires this file to be named config.yaml; every flow below uses .yml
appId: com.earthapp.sky

# _shared/ is deliberately excluded: those files are runFlow'd by the suites above
flows:
  - flows/smoke/*.yml
  - flows/native/*.yml

# report every lane, not just up to the first red one
executionOrder:
  continueOnFailure: true
